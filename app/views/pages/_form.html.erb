<%= form_with model: [ book, page ], id: "page-editor", format: :html, data: { controller: "autosave", action: "autosave#submit house-md:change->autosave#change" } do |form| %>
  <%= form.markdown_area :body, toolbar: "house_toolbar", required: true, autofocus: true, class: "page__editor" %>
<% end %>
